# Preregistered decision gates

- **G0 chain lock:** commits `0d2866b` and `5edb408` are ancestors; broad exact
  claims remain blocked.
- **P1 trace:** enough endogenous one-slip events are observed at a nondegenerate
  rate in the standard-derived VV trace.
- **P2 orbit:** both structured codes satisfy the frozen exact-collision limit.
- **P3 trigger:** the exact-binomial false-alarm upper bound and slip-detection
  lower bound pass.
- **P4 matched recovery:** one LS mode survives full all-state, posterior-pruned, and state-marginal code-specific baselines in each structured code, either through a speed win at compatible reliability or a resolved reliability win with bounded slowdown.
- **P5 no-slip:** median and p99 normal-frame overhead remain within limits.
- **P6 observed mixture:** the candidate retains a conditional p99 advantage
  when weighted by the observed trace event rate.
- **P7 controls:** trace-weighted LS goodput matches or exceeds the optimistic
  pilot bound; DQPSK is reported.
- **P8 reproducibility:** tests, hashes, schemas, seeds, caps, and validation pass.

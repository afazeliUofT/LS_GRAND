# Review handoff

Review in this order:

1. `FINAL_G3P_VERDICT.json` and `gate_status.csv`;
2. `code_orbit_summary.csv` and selected code artifacts;
3. `detector_summary.csv` and `no_slip_overhead.csv`;
4. `performance_comparisons.csv` and raw paired trials;
5. `physical_mixture.csv`;
6. `FROZEN_CONFIG.json`, manifests, tests, and validation.

Do not infer field-defining status from a positive precursor result. The only
permitted positive interpretation is that a final physical/matched-receiver gate
is worth executing.
